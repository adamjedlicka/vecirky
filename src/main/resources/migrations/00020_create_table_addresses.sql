CREATE TABLE addresses (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    city TEXT NOT NULL,
    street TEXT NOT NULL,
    number_of_descriptive INTEGER NOT NULL,
    zip_code INTEGER NOT NULL
);
